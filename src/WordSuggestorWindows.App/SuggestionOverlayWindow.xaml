<Window x:Class="WordSuggestorWindows.App.SuggestionOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WordSuggestor Suggestions"
        Width="292"
        Height="438"
        Background="Transparent"
        AllowsTransparency="True"
        ShowActivated="False"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStyle="None">
    <Window.Resources>
        <Style x:Key="OverlayHeaderButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Background" Value="#E7EDF5" />
            <Setter Property="BorderBrush" Value="#D0D9E6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#DDE7F4" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OverlayRowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="10"
            Padding="0"
            CornerRadius="14"
            Background="#F5F8FC"
            BorderBrush="#BFD0E4"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Padding="12,10,12,8"
                    Background="#EFF4FB"
                    CornerRadius="14,14,0,0"
                    BorderBrush="#D4DFEC"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="#2D3035"
                                       Text="Forslag" />
                            <TextBlock Margin="10,4,0,0"
                                       FontSize="12"
                                       Foreground="#7A8491"
                                       Text="{Binding SuggestionPageSummary}" />
                        </StackPanel>
                        <TextBlock Margin="0,4,0,0"
                                   FontSize="12"
                                   Foreground="#7A8491"
                                   Text="{Binding SuggestionPanelCountSummary}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                VerticalAlignment="Top">
                        <Button Style="{StaticResource OverlayHeaderButtonStyle}"
                                ToolTip="Statisk placering"
                                Click="StaticPlacementButton_OnClick">
                            <TextBlock FontSize="12"
                                       Text="📌" />
                        </Button>
                        <Button Style="{StaticResource OverlayHeaderButtonStyle}"
                                ToolTip="Follow-caret"
                                Click="FollowCaretButton_OnClick">
                            <TextBlock FontSize="12"
                                       FontWeight="SemiBold"
                                       Text="A|" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding VisibleSuggestions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource OverlayRowButtonStyle}"
                                    Click="SuggestionRow_OnClick">
                                <Border Margin="8,6,8,0"
                                        Padding="10,8"
                                        CornerRadius="10"
                                        BorderBrush="#D3DEEB"
                                        BorderThickness="1"
                                        Background="#F9FBFE">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Width="36"
                                                   Margin="0,2,8,0"
                                                   FontSize="12"
                                                   Foreground="#7A8491"
                                                   Text="{Binding ShortcutLabel}" />

                                        <StackPanel Grid.Column="1">
                                            <TextBlock FontSize="16"
                                                       FontWeight="SemiBold"
                                                       Foreground="#272B31"
                                                       Text="{Binding Suggestion.Term}" />
                                            <TextBlock Margin="0,3,0,0"
                                                       FontSize="12"
                                                       Foreground="#6F7986"
                                                       Text="{Binding Suggestion.Kind}" />
                                        </StackPanel>

                                        <TextBlock Grid.Column="2"
                                                   Margin="10,0,0,0"
                                                   VerticalAlignment="Top"
                                                   FontSize="12"
                                                   Foreground="#6F7986"
                                                   Text="{Binding Suggestion.Score, StringFormat=F2}" />
                                    </Grid>
                                </Border>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>

            <Border Grid.Row="2"
                    Padding="10,8"
                    CornerRadius="0,0,14,14"
                    Background="#EAF2FB"
                    BorderBrush="#D4DFEC"
                    BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Width="28"
                            Height="26"
                            Style="{StaticResource OverlayHeaderButtonStyle}"
                            Click="PreviousPageButton_OnClick">
                        <TextBlock FontFamily="Segoe Fluent Icons"
                                   FontSize="12"
                                   Text="&#xE76B;" />
                    </Button>

                    <StackPanel Grid.Column="1"
                                VerticalAlignment="Center">
                        <TextBlock FontSize="11"
                                   Foreground="#65707C"
                                   HorizontalAlignment="Center"
                                   Text="Klik eller Ctrl+1..Ctrl+0 vælger forslag" />
                        <TextBlock FontSize="11"
                                   Foreground="#65707C"
                                   HorizontalAlignment="Center"
                                   Text="Ctrl+Venstre/Højre skifter side" />
                    </StackPanel>

                    <Button Grid.Column="2"
                            Width="28"
                            Height="26"
                            Style="{StaticResource OverlayHeaderButtonStyle}"
                            Click="NextPageButton_OnClick">
                        <TextBlock FontFamily="Segoe Fluent Icons"
                                   FontSize="12"
                                   Text="&#xE76C;" />
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
